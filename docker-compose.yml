version: "3.8"

services:
  app:
    build: .
    ports:
      - "${PORT:-5001}:5001"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    env_file:
      - .env
    environment:
      - PORT=5001
    restart: unless-stopped
